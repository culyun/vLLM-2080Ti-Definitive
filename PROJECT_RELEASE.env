# Shared release metadata for scripts and runtime logs.
PROJECT_ROOT=${PROJECT_ROOT:-$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)}
PROJECT_NAME="vLLM 2080 Ti Definitive Edition"
PROJECT_AUTHOR="github.com/weicj"
RUNTIME_IDENTITY="vllm-sm75-tp2-cu128"
BASE_VLLM_VERSION="0.21.0"
VALIDATED_CUDA_VERSION="12.8"
VALIDATED_TORCH_BACKEND="cu128"
VALIDATED_TORCH_VERSION="2.11.0+cu128"
VALIDATED_TORCHAUDIO_VERSION="2.11.0+cu128"
VALIDATED_TORCHVISION_VERSION="0.26.0+cu128"
VALIDATED_NVIDIA_DRIVER_VERSION="590.48.01"
FORK_RELEASE=$(tr -d '[:space:]' < "$PROJECT_ROOT/VERSION")
